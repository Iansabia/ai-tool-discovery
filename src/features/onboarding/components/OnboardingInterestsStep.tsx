// src/features/onboarding/components/OnboardingInterestsStep.tsx
// Phase 2 / Plan 02-03 — STUB. Task 2 fills this in with the real chip grid + flow.
// Stub exists so the router split (Task 1) compiles before the real UI lands.
export function OnboardingInterestsStep() {
  return <div data-testid="onboarding-interests-stub">Loading interests step...</div>
}
